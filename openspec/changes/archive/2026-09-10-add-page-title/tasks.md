## 1. PageTitle reutilizable

- [x] 1.1 Crear `frontend/src/components/PageTitle.tsx` (`Typography` `h5`, `primary.dark`, `gutterBottom`, `children: ReactNode`) y usarlo en `RoomsPage.tsx`, `BookingsPage.tsx` y `LoginPage.tsx` en lugar del `Typography` actual, y verificar `npx tsc --noEmit` pasa en `/frontend`

## 2. Verificación

- [x] 2.1 Verificar `npm run build` pasa y vistazo manual a Salas, Mis reservas y Login (mismo texto, color y espaciado unificados)
