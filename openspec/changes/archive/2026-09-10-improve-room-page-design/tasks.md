## 1. Grid de salas

- [x] 1.1 Instalar `@mui/x-data-grid` y migrar `RoomsPage.tsx` (columnas ES, Chip Estado, IconButton+Tooltip editar solo admin, búsqueda única en cliente en vez de filtros) y verificar `npx tsc --noEmit` pasa
- [x] 1.2 Reordenar `RoomDialog.tsx` (nombre → ubicación → fila capacidad+switch, padding anti-corte) y verificar `npm run build` pasa

## 2. Verificación

- [x] 2.1 Revisar manual `/rooms` como admin y user: headers ES, chips, tooltip, búsqueda por cualquier valor, dialog crear/editar con orden nuevo y sin labels cortados
